function PageHeader() {
  return (
    <header className="page-header">
      <p className="eyebrow">Narrative Intelligence Pipeline Engine</p>
      <h1>NIPE Control Deck</h1>
      <p>
        Run the current backend-capable pipeline end-to-end: project setup, ingestion, mode selection, character map,
        voice settings, execution, and export validation.
      </p>
    </header>
  );
}

export default PageHeader;
